var i = 0;
var resultado = 0.0;

 for(i; i<=4; i++){

    var soma = prompt("Digite o Valor");
    soma = parseFloat(soma);

    resultado += soma ;

    //document.write(notas);

 }
 document.write(resultado);